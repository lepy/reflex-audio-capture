[project]
name = "faster-whisper-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "reflex>=0.6.6",
  "reflex-audio-capture",
  "jinja2>=3.1",
  "faster-whisper",
]

[tool.uv]
package = false
